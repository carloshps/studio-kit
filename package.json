{
  "name": "studio-kit",
  "version": "0.0.1",
  "private": true,
  "description": "CarlosHPS Studio — design system, component registry e templates",
  "scripts": {
    "dev": "pnpm --filter kit-site dev",
    "build": "pnpm --filter @studio/tokens build && pnpm --filter @studio/tailwind-preset build && pnpm --filter @studio/ui build",
    "build:registry": "node scripts/build-registry.mjs",
    "build:registry:dev": "node scripts/build-registry.mjs --base-url http://localhost:4321",
    "build:docker": "docker build -f apps/kit-site/Dockerfile -t kit-site .",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@9.0.0"
}
